# Vídeos

Coloca aquí los vídeos referenciados desde `src/config/gymkanaConfig.ts`:

- `day8-animo.mp4` — vídeo de ánimo del Día 8 (`videoSrc`)
- Los vídeos de amigos del Día 11 (`friendVideos[].videoSrc`), ej. `ana.mp4`

Si un vídeo no existe, se muestra automáticamente un placeholder elegante en su lugar.
